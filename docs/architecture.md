# Architecture

`card-mizer`는 도메인 규칙을 중심에 두고 외부 의존성을 포트와 어댑터로 분리하는 방향으로 설계한다.

## 원칙

- `card-core`는 Spring, JPA 같은 프레임워크에 직접 의존하지 않는다.
- API, 스케줄러, 외부 카드사 연동은 모두 어댑터 계층으로 분리한다.
- 추천 로직과 실적 계산 로직은 도메인 안에서 설명 가능해야 한다.

## 현재 단계

- 멀티모듈 구조를 먼저 고정
- 초기 도메인 타입과 포트 인터페이스 정의
- 이후 어댑터 구현과 테스트 전략 추가
